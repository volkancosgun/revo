import { Role } from './role';

export class User {
    id: number;
    unumber:string;
    name: string;
    email: string;
    password: string;
    status: number;
    role: Role;
    unote: string;
}